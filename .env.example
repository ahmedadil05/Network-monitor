# Flask application secrets (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-secret-key-here

# Database
DATABASE_PATH=backend/database/network_monitor.db

# Flask environment
FLASK_ENV=development
FLASK_DEBUG=0
